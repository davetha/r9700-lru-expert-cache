--- a/vllm/models/qwen4_exp/amd/indexer_qsa.py
+++ b/vllm/models/qwen4_exp/amd/indexer_qsa.py
@@ -4,17 +4,53 @@
 
 from __future__ import annotations
 
+import inspect
+import os
 from typing import cast
 
 import torch
 from torch import nn
 
 from vllm.config import VllmConfig
+from vllm.logger import init_logger
 from vllm.forward_context import get_forward_context
 from vllm.model_executor.layers.layernorm import GemmaRMSNorm
 from vllm.model_executor.layers.linear import ReplicatedLinear
 from vllm.model_executor.layers.quantization import QuantizationConfig
 from vllm.model_executor.layers.rotary_embedding.mrope import triton_mrope
+
+try:
+    # Pre-split cos/sin cache helper; only exists in the patched mrope.py. An ImportError
+    # here must not take the whole QSA indexer module down when that mount is absent.
+    from vllm.model_executor.layers.rotary_embedding.mrope import cos_sin_halves
+except ImportError:  # stock mrope.py: reproduce the original chunk() path
+    def cos_sin_halves(rope, x):
+        cache = rope._match_cos_sin_cache_dtype(x)  # noqa: SLF001
+        half = cache.shape[-1] // 2
+        return cache[..., :half], cache[..., half:]
+
+
+logger = init_logger(__name__)
+
+# Fold the cos/sin gather into the mrope kernel (k4 hitlist #12). Off by default;
+# `triton_mrope` only grows a `positions=` keyword in the patched mrope.py, so the
+# gate stays dead when that mount is absent.
+_ROPE_GATHER = os.environ.get("VLLM_QSA_ROPE_GATHER", "0") == "1"
+_ROPE_GATHER_OK: bool | None = None
+
+
+def _rope_gather_supported() -> bool:
+    global _ROPE_GATHER_OK
+    if _ROPE_GATHER_OK is None:
+        _ROPE_GATHER_OK = "positions" in inspect.signature(triton_mrope).parameters
+        if not _ROPE_GATHER_OK:
+            logger.warning(
+                "VLLM_QSA_ROPE_GATHER=1 but the mounted mrope.py has no gathering "
+                "triton_mrope; the QSA rope keeps the two gather kernels."
+            )
+    return _ROPE_GATHER_OK
+
+
 from vllm.transformers_utils.configs.qwen4_exp import (
     Qwen4ExpTextConfig,
 )
@@ -36,9 +72,14 @@
 
     num_tokens, _, head_dim = tensor.shape
     rotary_dim = rotary_emb.rotary_dim
-    cache = rotary_emb._match_cos_sin_cache_dtype(tensor)  # noqa: SLF001
-    cos_sin = cache[positions]
-    cos, sin = cos_sin.chunk(2, dim=-1)
+    # Gather from the pre-split cache: two contiguous gathers instead of one
+    # gather plus the two .contiguous() copies triton_mrope would otherwise make.
+    cos_cache, sin_cache = cos_sin_halves(rotary_emb, tensor)
+    gather = positions.ndim == 2 and _ROPE_GATHER and _rope_gather_supported()
+    if gather:
+        cos, sin = cos_cache, sin_cache
+    else:
+        cos, sin = cos_cache[positions], sin_cache[positions]
     if positions.ndim == 2:
         shape = tensor.shape
         tensor, _ = triton_mrope(
@@ -51,6 +92,7 @@
             rotary_dim,
             rotary_emb.mrope_interleaved,
             rotary_emb.is_neox_style,
+            positions=positions if gather else None,
         )
         return tensor.reshape(shape)
 
